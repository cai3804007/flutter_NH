import 'package:flutter/material.dart';

class PostIndexLatest extends StatelessWidget {
  const PostIndexLatest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.explore_outlined,
      size: 128,
      color: Colors.black12,
    );
  }
}
